# Keep values wrapped in single quotes, as dotenv will treat $VAR as a variable substitution.

# AI credits!
OPENAI_API_KEY='sk-xxxxxxxxxxx...'

# Name of the docker image to build/run
DOCKER_IMAGE=ponelat/askanybook

# For production only...
# Generate this with rails secret and paste it in 
SECRET_KEY_BASE=SECRET_KEY_BASE
